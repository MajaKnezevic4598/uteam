import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    litherBlack: "#101010",
    darkWhite: "#F8F8F8",
  },
  fonts: {
    body: "Mononoki",
  },
});

export default theme;
